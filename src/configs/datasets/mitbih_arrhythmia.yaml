train:
  _target_: src.datasets.MITBIHArrhythmiaDataset
  root: "data/raw/mitbih"
  split: "train"
  window_seconds: 10
  target_hz: 250
  stride_seconds: 5
  normalize: true
  train_val_ratio: 0.9
  instance_transforms: ${transforms.instance_transforms.train}
val:
  _target_: src.datasets.MITBIHArrhythmiaDataset
  root: "data/raw/mitbih"
  split: "val"
  window_seconds: 10
  target_hz: 250
  stride_seconds: 5
  normalize: true
  train_val_ratio: 0.9
  instance_transforms: ${transforms.instance_transforms.inference}
test:
  _target_: src.datasets.MITBIHArrhythmiaDataset
  root: "data/raw/mitbih"
  split: "test"
  window_seconds: 10
  target_hz: 250
  stride_seconds: 5
  normalize: true
  train_val_ratio: 0.9
  instance_transforms: ${transforms.instance_transforms.inference}
