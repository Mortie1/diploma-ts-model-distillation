train:
  _target_: src.datasets.InsectWingbeatDataset
  root: "data/raw/ucr"
  split: "train"
  normalize: true
  instance_transforms: ${transforms.instance_transforms.train}
val:
  _target_: src.datasets.InsectWingbeatDataset
  root: "data/raw/ucr"
  split: "test"
  normalize: true
  instance_transforms: ${transforms.instance_transforms.inference}
test:
  _target_: src.datasets.InsectWingbeatDataset
  root: "data/raw/ucr"
  split: "test"
  normalize: true
  instance_transforms: ${transforms.instance_transforms.inference}
