defaults:
  - /task: classification
  - /model: student_classification
  - /metrics: classification
  - /datasets: synthetic_classification
  - /dataloader: smoke
  - /transforms: plain
  - _self_

inferencer:
  device_tensors: ${task.device_tensors}
  device: auto
  save_path: "classification_predictions"
  seed: 42
  from_pretrained: "saved/classification_baseline/model_best.pth"
